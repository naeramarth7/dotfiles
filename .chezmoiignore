key.txt.age

{{ if ne .env.type "macos" -}}
brew/
Library/
{{- end }}

{{ if ne .env.type "macos" -}}
**/*_osx.src
{{- end }}

{{ if ne .env.type "macos" -}}
bin/alfred-*
bin/samba-fix
{{- end }}

{{ if ne .env.type "macos" -}}
.config/sketchybar
{{- end }}

{{ if ne .env.type "arch" -}}
.config/niri
.config/fuzzel
{{- end }}

# temp
.vim
