# The packages for each backend in group files can come in two formats, short-form
# and long-form:
#
# short-form syntax is simply a string of the name of the package.
#
# long-form syntax is a table which contains several fields which can
# optionally be set to specify install options on a per-package basis.
# The "package" field in the table specifies the name of the package.
#
# For example, the following two packages are equivalent:
# arch = [
#  "metapac",
#  { package = "metapac" }
# ]

{{ if eq .env.type "macos" -}}
brew = [
{{- else if eq .env.type "arch" -}}
arch = [
{{-  end }}
  # common
  "1password",
  "1password-cli",
  "bat",
  "diff-so-fancy",
  "git",
  "git-lfs",
  "gitleaks",
  "go-task",
  "gnupg",
  "helm",
  "htop",
  "jq",
  "mise",
  "pv",
  "screen",
  "sops",
  "tealdeer",
  "wget",
  "yq",
{{- if eq .env.type "macos" }}

  # macos only
  "alfred",
  "coreutils",
  "findutils",
  "git-recent",
  "moreutils",
  "openssh",
  "p7zip",
  "perl",
  "pinentry-mac", # gpg
  "watch",
{{- else if eq .env.type "arch" }}

  # arch only
  "fuzzel",
  "lucidglyph",       # font rendering hacks
  "git-recent-git",
  "noctalia-shell",
  "rclone",

  # niri
  "niri",
  "xwayland-satellite",
  "xdg-desktop-portal-gnome",
  "xdg-desktop-portal-gtk",
  "alacritty",
  "dms-shell-bin",
  "matugen",
  "wl-clipboard",
  "cliphist",
  "cava",
  "qt6-multimedia-ffmpeg",
  "mako", # notification manager

  # arch only fonts
  "inter-font",
  "ttf-roboto",
  "ttf-roboto-mono",
  "ttf-roboto-flex",
{{- end }}
]
cargo = [
  "cargo-binstall",
  "cargo-update",
  "metapac",
]
flatpak = []
mise = []
npm = []
