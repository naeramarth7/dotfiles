#!/bin/sh

# Use the following in the vagrant machines dir
# to copy the certs over
# vagrant ssh -c "sudo cp -r /var/lib/boot2docker/tls `pwd`/"

# Determine using `vagrant ssh-config`
export DOCKER_IP=localhost
export DOCKER_PORT=2375

export DOCKER_HOST=tcp://$DOCKER_IP:$DOCKER_PORT

# export DOCKER_TLS_VERIFY="1"
# export DOCKER_CERT_PATH="$HOME/vagrant-boxes/parallels-boot2docker/tls"

# export B2D_NFS_SYNC="1"
