#!/bin/sh

# Set docker env for dinghy
# taken from `dinghy shellinit`
use-dinghy () {
  eval $(dinghy env)
}

unuse-dinghy () {
  unset DOCKER_HOST
  unset DOCKER_CERT_PATH
  unset DOCKER_TLS_VERIFY
  unset DOCKER_MACHINE_NAME
}
